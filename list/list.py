mylist =   [1, 2, 3]
mylist.append('as')
print(mylist)

#.copy(), .clear(), .count (count the number of a value appear on a list 0 if dont exist), len()
# mylist[0],.pop(remove the las element of a list ), .remove(the lement on the index number pass as parameter)
# .reverse(), .sort(order the list must be the same type data)